<% if (type === 'object') { %>

  <% Object.keys(variable).forEach(function(variable) { %>

    <% include ./document %>

  <% }); %>

<% } else if (type === array) { %>

<% variable.forEach(function(variable) { %>

  include ./document

<% }); %>

<% } else { %>

  <%= variable %>

<% } %>
